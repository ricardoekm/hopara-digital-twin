export enum ResourceCacheKey {
  ICON = 'resource-icons',
  IMAGE = 'resource-images',
  MODEL = 'resource-models',
}

export enum ResourceType {
  ICON = 'icon',
  IMAGE = 'image',
  MODEL = 'model',
}
