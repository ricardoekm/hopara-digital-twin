export enum RowsetStatus {
  NONE = 'NONE',
  LOADING = 'LOADING', // When the fetching data is loading
  LOADED = 'LOADED', // When the fetching data is loaded
  ERROR = 'ERROR', // when has fetching error
}
