import React from 'react'
import {AttributionFooter} from './AttributionFooter'

export default {
    title: 'Canvas/Attribution',
    component: AttributionFooter,
}

const Template = (args) => <AttributionFooter {...args} />

export const Default = Template.bind({})
